function printGreetings(message = 'Default Parameter') {
  console.log(message);
}

printGreetings('New Parameter');

// Default parameters 
// When you don't pass anything to the function the function return undefined
// You can give a Default Parameter on the argument's name.
// And change this parameter when you call the function attributing a new parameter
// when you call.